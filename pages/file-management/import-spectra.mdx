import Image from 'next/image';
import FileManagementFooterLinks from '/components/FileManagementFooterLinks';

export const imagePath = '/images/file-management/';

## Import spectra

<hr />

<Image
  src={`${imagePath}import_spectra_icon.svg`}
  alt='delete spectra icon'
  width={65}
  height={65}
/>

Allows users to import spectra and its associated metadata into current workspace.

Suported files include:

| File Format                   | Description                                  |
| ----------------------------- | -------------------------------------------- |
| .spsp                         | Spectrify native file format                 |
| plain text _(.csv, .txt,...)_ | Row or Columns wise. Single or Multi spectra |
| jcamp-dx _(.jdx, .dx)_        | JCAMP Standard based files                   |

### Standard Importer

The import pop up allows to directly drag&drop files or navigate through your own Operating System files menu.

- Use the File Format dropdown menu to select wether your file is row or column wise.
- Headers (first row) or indexes (first column) must be numeric wavelengths.
- It autodetect is there are a single or multiple spectra.
- Metadata will be automatically imported based in if the field name is string.

<br />
<Image
  src={`${imagePath}file_format_selection.png`}
  alt='advanced importer activation'
  width={500}
  height={300}
  style={{ display: 'block', margin: '0 auto' }}
/>

### Advanced Importer

Activate the Advanced Importer button in the Import pop up menu to access Advanced Import options.

<br />
<Image
  src={`${imagePath}advanced_importer_activation.png`}
  alt='advanced importer activation'
  width={500}
  height={300}
  style={{ display: 'block', margin: '0 auto' }}
/>

The Advanced Import Options feature allows users to inspect and configure file contents, ensuring accurate data import and preventing conflicts.

The interface is divided into four key sections:

1. **Raw Content Preview** (Top-Left)

   - Displays the unprocessed document content.
   - Aids in selecting appropriate import settings.

2. **Settings Panel** (Top-Right)

   - Allows customization of import parameters:
     - Decimal separator selection.
     - Custom delimiter specification (beyond default comma).
     - Row skipping option for file headers or irrelevant data.

3. **Parsed Data View** (Bottom-Left)

   - Shows real-time interpretation of the file based on current settings.
   - Enables scrolling through initial spectra for verification.
   - Automatically identifies textual fields as metadata.

4. **Spectral Preview** (Bottom-Right)
   - Visualizes the first few spectra as they will appear in Spectrify.
   - Provides immediate visual feedback on import accuracy.

<Image
  src={`${imagePath}advanced_importer_menu.png`}
  alt='advanced importer menu example'
  width={900}
  height={300}
  style={{ display: 'block', margin: '0 auto' }}
/>

- **Apply To Current**: Will apply the settings to the current file, opening to the next one in case multiple files are loaded.
- **Apply to All Files**: Will apply same setting to all uploaded files.

### Import errors

An import error occurs due to an unexpected file format. Using Advanced Mode for file inspection may resolve this issue.

<Image
  src={`${imagePath}import_spectra_error.png`}
  alt='advanced importer menu example'
  width={500}
  height={300}
/>

<br />
<hr />
<br />

<FileManagementFooterLinks imagePath={imagePath} />
